# Resolve nvm-managed node into PATH for non-interactive shells (git hooks)
export NVM_DIR="$HOME/.config/nvm"
node_bin=$(ls -d "$NVM_DIR/versions/node/"* 2>/dev/null | sort -V | tail -1)/bin
export PATH="$node_bin:$PATH"

commitlint --edit $1
